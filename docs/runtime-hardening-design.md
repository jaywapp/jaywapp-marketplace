# 성능 및 안정성 설계

Dirent로 조회 비용을 줄이고 객체 입력을 검증한다. 생성 실패 시 기존 레지스트리를 보존하고 원인을 출력한다. node:test로 CLI 성공/실패와 기존 파일 보존을 검증한다.

정상 입력의 반환값·순서·출력 의미를 보존한다. 실패를 성공으로 숨기지 않는다. 외부 API/운영 DB를 테스트에 사용하지 않는다. 새로운 기능/의존성은 추가하지 않는다.
